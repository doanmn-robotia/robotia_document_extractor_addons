<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extraction Job Dialog Form (Clean version for failed jobs) -->
    <record id="view_queue_job_extraction_dialog_form" model="ir.ui.view">
        <field name="name">queue.job.extraction.dialog.form</field>
        <field name="model">queue.job</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <form string="Extraction Job Status">
                <header>
                    <field name="state" widget="statusbar"
                           statusbar_visible="wait_dependencies,pending,enqueued,started,done"
                           statusbar_colors='{"failed":"red","done":"green","cancelled":"yellow"}'/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="extraction_last_step" readonly="1"/>
                            <field name="date_created" readonly="1"/>
                        </group>
                        <group>
                            <field name="date_started" readonly="1"/>
                            <field name="date_done" readonly="1"/>
                        </group>
                    </group>

                    <!-- Error Details (with fallback to queue.job.exc_info) -->
                    <group string="Error Details" invisible="extraction_error == False">
                        <field name="extraction_error" nolabel="1" readonly="1"
                               widget="text"
                               class="text-danger"
                               style="font-family: monospace; white-space: pre-wrap;"/>
                    </group>
                </sheet>

                <!-- Footer with action buttons -->
                <footer>
                    <button name="requeue"
                            string="Requeue Job"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'failed'"
                            groups="queue_job.group_queue_job_manager"/>

                    <button name="button_done"
                            string="Set to Done"
                            type="object"
                            class="btn-secondary"
                            invisible="state not in ('pending', 'enqueued', 'failed')"
                            groups="queue_job.group_queue_job_manager"/>

                    <button name="button_cancelled"
                            string="Cancel Job"
                            type="object"
                            class="btn-secondary"
                            invisible="state not in ('pending', 'enqueued', 'failed')"
                            groups="queue_job.group_queue_job_manager"/>

                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
